class ChatAgent {
  final String id;
  final String title;
  final String systemInstruction;

  const ChatAgent({
    required this.id,
    required this.title,
    required this.systemInstruction,
  });
}
